// let studentNames =[]
// let studentNamesObject = new Array();
//  let String = ["JS","Python"]
//  let numbersArray = [10, 20, 30, 40, 50];
//  let booleanArray = [true, false, true, false];
//  let mixedArray = ["Hello", 42, true, { key: "value" }, [1, 2, 3]];
//  let qualifications = [
//     "SSC",
//     "HSC",
//     "BCS",
//     "BS",
//     "BCOM",
//     "MS",
//     "M. Phil.",
//     "PhD"
// ];

// document.write("<h1>Education Qualifications in Pakistan:</h1>");
// document.write("<ol>");
// for (let i = 0; i < qualifications.length; i++) {
//     document.write(`<li>${qualifications[i]}</li>`);
// }
// document.write("</ol>");

// --------------------------------------------------------------------------------------

//  let studentNames =["jannat","qurat","kainat"]
//  let studentScores =["320","230","480"]
//  const TotalMarks = 500

//  for(let i =0;i<studentNames.length;i++)
//  {
//     let percentage= (studentScores[i]/500)*100;
//     document.write(`<li>Score of${studentNames[i]} is ${studentScores[i]} .percentage is ${percentage} % </li>`);
// }

// --------------------------------------------------------------------------------------
// Initializing the array with color names
// let colors = ["Red", "Blue", "Green", "Yellow", "Orange"];

// // Display the initial array
// document.write("Initial colors array: <br>" + colors.join(", ") + "<br><br>");

// // a. Ask the user what color they want to add to the beginning & add that color
// let colorToAddToStart = prompt("What color would you like to add to the beginning of the array?");
// colors.unshift(colorToAddToStart);
// document.write("Array after adding color to the beginning: <br>" + colors.join(", ") + "<br><br>");

// // b. Ask the user what color they want to add to the end & add that color
// let colorToAddToEnd = prompt("What color would you like to add to the end of the array?");
// colors.push(colorToAddToEnd);
// document.write("Array after adding color to the end: <br>" + colors.join(", ") + "<br><br>");

// // c. Add two more colors to the beginning
// colors.unshift("Purple", "Pink");
// document.write("Array after adding two colors to the beginning: <br>" + colors.join(", ") + "<br><br>");

// // d. Delete the first color in the array
// colors.shift();
// document.write("Array after deleting the first color: <br>" + colors.join(", ") + "<br><br>");

// // e. Delete the last color in the array
// colors.pop();
// document.write("Array after deleting the last color: <br>" + colors.join(", ") + "<br><br>");

// // f. Ask the user at which index they want to add a color & color name
// let indexToAdd = prompt("At which index would you like to add a new color?");
// let colorToAdd = prompt("What color would you like to add at index " + indexToAdd + "?");
// colors.splice(indexToAdd, 0, colorToAdd);
// document.write("Array after adding color at the specified index: <br>" + colors.join(", ") + "<br><br>");

// // g. Ask the user at which index they want to delete color(s) & how many to delete
// let indexToDelete = prompt("At which index would you like to delete color(s)?");
// let numOfColorsToDelete = prompt("How many colors would you like to delete?");
// colors.splice(indexToDelete, numOfColorsToDelete);
// document.write("Array after deleting the specified number of colors: <br>" + colors.join(", ") + "<br><br>");

// --------------------------------------------------------------------------------------


// let studentScores = [450, 320, 400, 480, 390];

// studentScores.sort();

// document.write("Sorted student scores using default sort: <br>" + studentScores.join(", ") + "<br>");
// --------------------------------------------------------------------------------------
// Initializing the cities array with city names
// let cities = ["New York", "London", "Paris", "Tokyo", "Sydney"];

// // Copying the first three elements of the cities array to selectedCities array
// let selectedCities = cities.slice(0, 3);

// // Displaying the original cities array and the selectedCities array
// document.write("Original Cities Array: <br>" + cities.join(", ") + "<br><br>");
// document.write("Selected Cities Array (first 3 elements): <br>" + selectedCities.join(", ") + "<br>");

// --------------------------------------------------------------------------------------

// var arr = ["This ", " is ", " my ", " cat"];

// var singleString = arr.join("");  // Joining without any separator


// document.write("The combined string is: " + singleString);

// --------------------------------------------------------------------------------------

for (let i = 0; i < array.length; i++) {
    let inp= prompt("enter number");
    let ans[i] = input;
}
let ans2=ans=

// --------------------------------------------------------------------------------------
// --------------------------------------------------------------------------------------
